import { IRasterFormat, IBandsOperation } from '../../interface';
import { SourceTile } from '@antv/l7-utils';
export declare const getCustomData: (tile: SourceTile, getCustomData: (tile: {
    x: number;
    y: number;
    z: number;
}, cb: (err: any, data: any) => void) => void, rasterFormat: IRasterFormat, operation?: IBandsOperation) => Promise<unknown>;
